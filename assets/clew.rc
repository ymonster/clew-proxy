#include <windows.h>

// Embed the Clew icon as resource ID 1. Windows Explorer / taskbar use the
// resource with the numerically smallest ID as the executable's default icon,
// so ID 1 guarantees it wins.
1 ICON "clew.ico"
